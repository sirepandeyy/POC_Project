{
  "name": "React + Spring Boot with Docker",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "docker-in-docker": "latest"
  },
  "forwardPorts": [3000, 8080],
  "postCreateCommand": "docker-compose up --build -d",
  "remoteUser": "vscode"
}
